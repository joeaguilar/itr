# Blitz Plan: code-review-2026-05-19

## Config

- Tracker list: `itr list --tag code-review-2026-05-19 -f json`
- Tracker epic: #134 Blitz: code-review-2026-05-19
- Dep graph: `kgr` present; `kgr check` OK, no cycles or rule violations
- Verify gate: `cargo build && cargo test && cargo clippy --all-targets -- -D warnings && cargo fmt --all -- --check && ./tests/integration.sh ./target/debug/itr`
- Concurrency: 5
- Repos: `.`
- Stop when: backlog empty or 2 no-progress waves
- Tagged backlog: 10 open issues
- Recommended execution backlog: 9 issues, with #117 pulled forward as a Wave 1 gate intervention
- Recommended separate sprint: #95 docs v1 polish epic

## Waves

### Wave 1

- #116 Make init and schema reflect the current migrated database shape
  - Owns: `src/db.rs`, `src/commands/init.rs`, `src/commands/schema.rs`, `tests/integration.sh`
  - Reason: foundational DB/schema behavior should land before later DB behavior tests.
- #80 Harden Windows installer checksum error handling
  - Owns: `install.ps1`
  - Reason: independent installer script.
- #81 Detect BREAKING CHANGE footers in auto-version
  - Owns: `.github/workflows/auto-version.yml`
  - Reason: independent release workflow.

### Wave 2

- #115 Make add with blocked_by validate and rollback atomically
  - Owns: `src/commands/add.rs`, `src/db.rs`, `tests/integration.sh`
  - Reason: dependency creation behavior before dependency cleanup behavior.

### Wave 3

- #114 Clean dependency edges when update paths resolve blockers
  - Owns: `src/commands/update.rs`, `src/commands/batch.rs`, `src/commands/bulk.rs`, `src/db.rs`, `tests/integration.sh`
  - Reason: depends semantically on stable dependency creation and shared dependency helpers.

### Wave 4

- #112 Fix search so FTS does not hide note-only matches
  - Owns: `src/commands/search.rs`, `src/db.rs`, `tests/integration.sh`
  - Reason: isolated search semantics, but conflicts on DB and integration tests.

### Wave 5

- #113 Keep ready output non-terminal with explicit status filters
  - Owns: `src/commands/ready.rs`, `src/db.rs`, `tests/integration.sh`
  - Reason: isolated ready/listing semantics, but conflicts on DB and integration tests.

### Wave 6

- #117 Normalize integration test binary path before temp-directory cd
  - Owns: `tests/integration.sh`, `AGENTS.md`, `docs/testing.md`
  - Reason: test harness fix should precede release smoke workflow work.
  - Status: closed early as Wave 1 gate intervention.

### Wave 7

- #79 Add release smoke tests before uploading artifacts
  - Owns: `.github/workflows/release.yml`, `tests/integration.sh`
  - Reason: release workflow should build on the normalized test binary path.

### Separate sprint candidate

- #95 Docs v1 polish - post-verification sweep
  - Owns: `README.md`, `CLAUDE.md`, `docs/`
  - Reason: this is an epic, not a single implementation issue. It has many child issues listed in its note (#118-#133 plus #93 and #94), broad ownership over `docs/`, and acceptance depends on a second verification pass. It should be planned as a docs sprint instead of closed inside this code blitz.

## File conflicts

- `tests/integration.sh`: #79, #112, #113, #114, #115, #116, #117
- `src/db.rs`: #112, #113, #114, #115, #116
- `docs/`: #95 broad ownership conflicts with #117 `docs/testing.md`
- `.github/workflows/`: #79 and #81 are semantic release neighbors but do not share a file

## Semantic warnings

- #116 should run before the other DB-owning issues because it changes the baseline schema/init behavior that later integration tests assume.
- #115 and #114 are a dependency lifecycle pair. #115 should avoid changing `db::add_dependency` contracts unexpectedly; #114 should reuse existing blocker cleanup/reporting helpers where possible.
- #112 must preserve FTS creation and reindex behavior after #116 updates schema/init behavior.
- #117 should run before #79 because #79 may rely on the documented `./tests/integration.sh ./target/debug/itr` invocation.
- #95 should not be run as a normal wave task. Its acceptance criteria require child issue closure and a follow-up documentation verification pass.

## Interventions

- Wave 1 gate: #80, #81, and #116 each completed implementation but could not close because the exact approved gate failed at `./tests/integration.sh ./target/debug/itr`.
- Pulled #117 forward from Wave 6 and fixed `tests/integration.sh` path normalization plus `AGENTS.md` and `docs/testing.md` docs.
- Reran the exact approved gate outside the sandbox for localhost UI binding: `cargo build && cargo test && cargo clippy --all-targets -- -D warnings && cargo fmt --all -- --check && ./tests/integration.sh ./target/debug/itr`. Result: green, integration `283 passed, 0 failed`.

## Outcomes

- #116 closed: fresh init/schema now reflect migrated schema; full gate passed.
- #80 closed: checksum download failures warn/skip across PowerShell exception types; mismatches still fail; full gate passed.
- #81 closed: auto-version checks commit bodies/footers for BREAKING CHANGE; full gate passed.
- #117 closed early: integration script normalizes explicit binary paths before temp-directory cd; docs updated; full gate passed.
- #115 closed: add --blocked-by validation is atomic; malformed tokens are reviewed; missing numeric blocker IDs roll back created issues; full gate passed, integration `291 passed, 0 failed`.
- #114 closed: update, batch update, and bulk update terminal-status paths remove blocker edges while preserving newly-unblocked reporting; full gate passed, integration `302 passed, 0 failed`.
- #112 closed: FTS-backed search now appends LIKE-only note matches without duplicating FTS-ranked IDs; full gate passed, integration `304 passed, 0 failed`.
- #113 closed: ready output remains non-terminal even with explicit terminal status filters; full gate passed, integration `308 passed, 0 failed`.
- #79 closed: release workflow smoke-tests native release binaries before packaging/upload; full gate passed, integration `308 passed, 0 failed`.

## Final Report

### Outcomes by wave

- Wave 1: #116 closed, #80 closed, #81 closed.
- Wave 2: #115 closed.
- Wave 3: #114 closed.
- Wave 4: #112 closed.
- Wave 5: #113 closed.
- Wave 6: #117 closed early as Wave 1 gate intervention.
- Wave 7: #79 closed.
- Deferred: #95 remains open as its own docs sprint.

### Files touched per task

- #116: `src/db.rs`, `tests/integration.sh`
- #80: `install.ps1`
- #81: `.github/workflows/auto-version.yml`
- #117: `tests/integration.sh`, `AGENTS.md`, `docs/testing.md`
- #115: `src/commands/add.rs`, `tests/integration.sh`
- #114: `src/commands/update.rs`, `src/commands/batch.rs`, `src/commands/bulk.rs`, `tests/integration.sh`
- #112: `src/commands/search.rs`, `tests/integration.sh`
- #113: `src/commands/ready.rs`, `tests/integration.sh`
- #79: `.github/workflows/release.yml`, `tests/integration.sh`

### Wave timeline

- Started: 2026-05-19T03:36:23Z
- Wave 1 gate: green, integration `283 passed, 0 failed`
- Wave 2 gate: green, integration `291 passed, 0 failed`
- Wave 3 gate: green, integration `302 passed, 0 failed`
- Wave 4 gate: green, integration `304 passed, 0 failed`
- Wave 5 gate: green, integration `308 passed, 0 failed`
- Final gate after Wave 7: green, integration `308 passed, 0 failed`

### Quarantine triage notes

- None. No task required quarantine.

### Diff summary

- `.github/workflows/auto-version.yml`: breaking-change footer detection.
- `.github/workflows/release.yml`: native release binary smoke test before packaging/upload.
- `install.ps1`: checksum download failure warning behavior broadened while checksum mismatches still fail.
- `src/db.rs`: base schema now includes migrated objects; init/open share current migrations.
- `src/commands/add.rs`: atomic add/dependency transaction and invalid `blocked_by` review handling.
- `src/commands/update.rs`, `src/commands/batch.rs`, `src/commands/bulk.rs`: terminal status updates remove blocker edges.
- `src/commands/search.rs`: FTS-ranked search appends LIKE-only note matches.
- `src/commands/ready.rs`: ready output filters out terminal issues even with explicit terminal status filters.
- `tests/integration.sh`: smoke mode plus coverage for all fixed behaviors.
- `AGENTS.md`, `docs/testing.md`: explicit binary path normalization note.

### Next steps

- Review and commit the code changes.
- Plan #95 as a docs-focused sprint.

## Pre-Commit Review Disposition

- Fixed: search now only runs the full LIKE fallback when FTS is unavailable or empty; FTS-hit merges only note candidates.
- Fixed: fresh init now attempts to create the FTS virtual table and integration tests assert it exists.
- Fixed: single-item update now applies field updates, unblocked reporting, and blocker-edge cleanup in one transaction.
- Fixed: `install.ps1` only skips checksum verification for HTTP 404 checksum assets; other checksum download errors still fail.
- Fixed: bulk terminal-status cleanup uses a `HashSet` for unblocked dedupe.
- Kept as-is: `ready --status done|wontfix` returns an empty ready list without `REVIEW:` because the status is valid input and the result is a valid empty intersection with the non-terminal ready contract.
- Final pre-commit gate: green, integration `309 passed, 0 failed`.
